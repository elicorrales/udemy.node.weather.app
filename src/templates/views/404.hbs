<!DOCTYPE html>
<html>
    <head>
        {{>head}}
        <title>{{title}}</title>
    </head>
    <body>
        <div class="main-content">
            {{>pageheader}}
            <h1><strong>404</strong> {{message}} </h1>
        </div>
        {{>pagefooter}}
    </body>
</html>