<!DOCTYPE html>
<html>
    <head>
        {{>head}}
        <title>{{title}}</title>
    </head>
    <body>
        <div class="main-content">
            {{>pageheader}}
            <h1><strong>500</strong>
            </h1>
            <h3>
            {{message}}
            </h3>
        </div>
        {{>pagefooter}}
    </body>
</html>